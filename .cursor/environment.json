{
  "name": "my-profile (Jekyll + LaTeX CV)",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "bundle config set --local path 'vendor/bundle' && bundle install",
  "terminals": [
    {
      "name": "jekyll-serve",
      "command": "bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload",
      "description": "Jekyll dev server for the profile site (http://localhost:4000/my-profile/)."
    }
  ],
  "ports": [
    {
      "name": "jekyll",
      "port": 4000
    }
  ]
}
